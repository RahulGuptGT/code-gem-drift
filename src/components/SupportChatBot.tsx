import { SupportChatDrawer } from "@/components/support-chat/SupportChatDrawer";

// Thin wrapper kept for backward-compatible imports (Layout.tsx, App.tsx)
const SupportChatBot = () => <SupportChatDrawer />;

export default SupportChatBot;
